/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your app can stay default. Middleware handles the redirect.
  // Note: For production, run behind a proxy that sets correct Host headers.
};

module.exports = nextConfig;
